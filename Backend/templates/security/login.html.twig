<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}S'identifier{% endblock %} - Excellence Soutien Solaire</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        

    </head>
    <body>
{% block body %}
    <div class="login-container">
        <div class="login-box">
            <div class="login-content">
                <h1>S'identifier</h1>
                <form method="post">
                    <label for="inputEmailGerant">Email</label>
                    <input type="email" value="{{ last_username }}" name="emailGerant" id="inputEmailGerant" class="form-control" required autofocus>
                    
                    <label for="inputPassword">Mot de passe</label>
                    <div class="password-container">
                        <input type="password" name="password" id="inputPassword" class="form-control password-field" required>
                        <i class="fa-solid fa-eye" id="togglePassword"></i>
                    </div>
                    
                    {# <a href="#" class="forgot-password">Mot de passe oublié ?</a> #}
                    
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    
                    <button class="btn-login" type="submit">S'IDENTIFIER →</button>
                </form>
            </div>
            <div class="login-image-container">
                <img src="{{ asset('assets/img/gerantConnexion.png') }}" alt="gerantConnexion" class="login-image">
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    <style>
    @import url('https://fonts.googleapis.com/css2?family=Anton&family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');
        /* Style global */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f9f6f4;
            margin: 0;
            font-family: "Montserrat", sans-serif;

        }

        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }

        .login-box {
            display: flex;
            background: #fff;
            border-radius: 55px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 900px;
            height: 550px;
            position: relative;
        }

        /* Section formulaire */
        .login-content {
            padding: 50px;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .login-content h1 {
            font-size: 35px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
            font-family: "Anton", sans-serif;

        }

        /* Champs de saisie */
        .input-group {
            width: 85%;
            display: flex;
            flex-direction: column;
            margin-bottom: 15px;
        }

        .input-group label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .form-control {
            width: 95%;
            padding: 14px;
            background: #FCDDD4;
            font-size: 16px;
            box-sizing: border-box;
            border: none;
            margin-bottom: 22px;
            height: 50px; 
            font-family: "Montserrat", sans-serif;

        }

        .form-control:focus {
            outline: none;
            border-color: #FCDDD4;
        }

        /* Gestion du mot de passe */
        .password-container {
            position: relative;
            width: 95%;
        }

        .password-field {
            width: 100%;
            padding-right: 40px;
            height: 50px; 
        }

        #togglePassword {
            position: absolute;
            right: 15px;
            top: 40%; 
            transform: translateY(-50%);
            cursor: pointer;
            color: #FF5757;
            font-size: 14px; 

        }


        /* Mot de passe oublié */
        .forgot-password {
            font-size: 14px;
            color: black;
            text-decoration: none;
            display: block;
            text-align: right;
            width: 95%;
            margin-bottom: 20px;
        }

        /* Bouton Connexion */
        .btn-login {
            background-color: #F87E5F;
            color: white;
            border: none;
            padding: 15px;
            border-radius: 30px;
            cursor: pointer;
            font-size: 14px;
            font-weight: bold;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            width: 40%;
            font-family: "Montserrat", sans-serif;

        }

        .btn-login:hover {
            background: #cf6a58;
        }

        /* Image */
        .login-image-container {
            width: 300px;
            background: #FF5757;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            border-top-right-radius: 55px;
            border-bottom-right-radius: 55px;
        }

        .login-image {
            position: absolute;
            left: -147px;
            height: 548px;
            pointer-events: none;

        }

        /* Responsive */
        @media (max-width: 768px) {
            .login-box {
                flex-direction: column-reverse;
                width: 90%;
                height: auto;
            }

            .login-image-container {
                width: 100%;
                height: 200px;
                border-radius: 0;
            }

            .login-image {
                left: 0;
                height: 100%;
                object-fit: contain;
            }
        }
    </style>
{% endblock %}


{% block javascripts %}
    <script>
        document.getElementById("togglePassword").addEventListener("click", function () {
            let passwordField = document.getElementById("inputPassword");
            if (passwordField.type === "password") {
                passwordField.type = "text";
                this.classList.remove("fa-eye");
                this.classList.add("fa-eye-slash");
            } else {
                passwordField.type = "password";
                this.classList.remove("fa-eye-slash");
                this.classList.add("fa-eye");
            }
        });
    </script>
{% endblock %}


    </body>
</html>



